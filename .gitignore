# Dependency directories
node_modules/

# Build output
# NOTE: build/ is intentionally tracked so that `npx github:OctavioCriollo/obs-mcp`
# can execute the compiled code directly without requiring a TypeScript build step
# on the consumer's machine. Keep build/ in sync with src/ when committing.
dist/

# Environment variables
.env

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS specific
.DS_Store
Thumbs.db
